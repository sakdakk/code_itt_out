#include <bits/stdc++.h>
 
using namespace std;
typedef long long ll;
int main() {
ll n;
cin>>n;
ll a[n];
for(ll i=0;i<n;i++)
cin>>a[i];
sort(a,a+n);
ll mid=a[n/2];
ll s=0;
for(ll i=0;i<n;i++)
{
   s+=abs(mid-a[i]) ;
}
//s+=mid
cout<<s<<"\n";
}
